interface:
  display_name: "Superpowers"
  short_description: "Repo-specific patterns and workflows for superpowers"
  default_prompt: "Use the superpowers repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true